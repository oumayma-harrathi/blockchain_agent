version: '3.8'

services:
  blockchain-agent:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./chroma_db:/app/chroma_db
      - ./data/blockchains.json:/app/blockchains.json
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434