# Core packages
streamlit==1.32.0
pandas==2.1.4
numpy==1.26.4
requests==2.31.0
python-dotenv==1.0.1
colorlog==6.8.2

# Langchain and related
langchain==0.3.27
langchain-core==0.3.72
langchain-community==0.3.27
langchain-ollama==0.3.6
langchain-huggingface==0.1.2
langchain-chroma
langchain-text-splitters==0.3.9

# Vector DB and embeddings
chromadb==0.5.15
sentence-transformers==3.0.1
huggingface-embeddings  # (if needed – not on PyPI, must be in extra pip line if custom)

# NLP & Search
spacy==3.7.2
duckduckgo-search==6.2.13
ddg-scraper  # (if not available via PyPI, keep in separate pip install)
tiktoken==0.7.0

# Typing / Validation
pydantic>=2.9.0
typing-extensions==4.12.2
